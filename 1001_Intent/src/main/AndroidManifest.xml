<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.tcnr08.m1001">

<uses-permission android:name="android.permission.CALL_PHONE">允許打電話</uses-permission>
<uses-permission android:name="android.permission.CALL_COMPANION_APP">允許打電話</uses-permission>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher1001"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher1001_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".M1001"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>